# name: 1.0.$(BuildID)

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - infra/*

pool:
  vmImage: 'ubuntu-latest'

stages:
  - stage: Build
    displayName: "Infra Validatation"
    jobs:
      - template: InfraDeployTemplates/infravalidation.yml
        parameters:
          environment: "dev"
          serviceconnection: "svc-con"

  - stage: Dev
    displayName: "Dev"
    jobs:
      - template: InfraDeployTemplates/deployresources.yml
        parameters:
          environment: "dev"
          serviceconnection: "svc-con"