parameters:
- name: "acrServiceConnection"
  type: string
- name: "imageName"
  type: string
- name: "dockerFilePath"
  type: string
- name: "tag"
  type: string

jobs:
  - job: Build
    displayName: Build Job
    steps:      
      - task: Docker@2
        displayName: Build and publish job to ACR
        inputs:
          command: buildAndPush
          containerRegistry: ${{parameters.acrServiceConnection}}
          repository: ${{parameters.imageName}}
          Dockerfile: ${{parameters.dockerFilePath}}
          tags: |
            ${{parameters.tag}}

